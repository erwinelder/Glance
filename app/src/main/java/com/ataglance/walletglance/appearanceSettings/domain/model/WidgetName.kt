package com.ataglance.walletglance.appearanceSettings.domain.model

/*
Enum names are connected with the database.
After changing some of the names listed below, create a proper database migration.
*/
enum class WidgetName {
    ChosenBudgets,
    TotalForPeriod,
    RecentRecords,
    TopExpenseCategories
}